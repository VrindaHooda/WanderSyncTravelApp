<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/vacationTimeInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Duration" />

    <!-- Button to open the start date picker -->
    <Button
        android:id="@+id/openStartDatePicker2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Start Date" />

    <!-- TextView to display the selected start date -->
    <TextView
        android:id="@+id/startDateText2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Date: Not Selected"
        android:paddingTop="10dp" />

    <!-- Button to open the end date picker -->
    <Button
        android:id="@+id/openEndDatePicker2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select End Date" />

    <!-- TextView to display the selected end date -->
    <TextView
        android:id="@+id/endDateText2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="End Date: Not Selected"
        android:paddingTop="10dp" />

    <!-- TextView to display the calculated time -->
    <TextView
        android:id="@+id/calculated_time_display"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calculated time will be displayed here"
        android:paddingTop="10dp"
        android:textSize="16sp"
        android:textColor="@android:color/black" />

    <!-- Horizontal LinearLayout to contain the Submit and Display Calculated Time buttons side by side -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:paddingTop="16dp">

        <Button
            android:id="@+id/submitVacationTimeButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/display_calculated_time_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Display Calculated Time" />
    </LinearLayout>

</LinearLayout>
